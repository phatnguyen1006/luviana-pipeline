import Apartment from "../models/address.model.js";

const createApartment = async (data) => {
    try {

    } catch (err) {
        console.log(err);
        throw err;
    }
}

export default createApartment;